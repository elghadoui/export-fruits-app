# Export Fruits App

Application de gestion de facturation export (agrume + fruits rouges).

## Structure

- `backend-api/` : API ASP.NET Core (.NET 8) + EF Core
- `frontend-react/` : Application React (Vite + Tailwind)
- `sql/` : Scripts SQL pour la base `export_db`
- `docs/` : MCD, schémas, notes fonctionnelles

## Environnement

- .NET 8 SDK
- Node.js 20+
- MySQL 8
- Git
